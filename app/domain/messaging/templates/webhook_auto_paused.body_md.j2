Crewday paused **{{ subscription_name }}** because its last {{ observed_deliveries }} delivery attempts over {{ webhook_health_window_h }} hours all failed.

Re-enable the webhook after the receiver is healthy. Dropped deliveries are not replayed automatically.
