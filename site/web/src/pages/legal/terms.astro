---
import LegalMarkdownPage from "@/components/LegalMarkdownPage.astro";
import TermsDocument, { frontmatter } from "@/content/en/legal/terms.md";

const page = frontmatter;
---

<LegalMarkdownPage
  title={page.title}
  description={page.description}
  eyebrow={page.eyebrow}
  heading={page.heading}
  intro={page.intro}
>
  <TermsDocument />
</LegalMarkdownPage>
